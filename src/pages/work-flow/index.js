import React, { Component } from 'react'
import { connect } from 'react-redux'

class WorkFlow extends Component {
  render() {
    return (
      <div>Need to use GGEditor</div>
    )
  }
}


export default connect()(WorkFlow)